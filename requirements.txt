torch>=2.0.0
numpy>=1.24
pandas>=2.0
statsmodels>=0.14       # ARIMA baseline
pyarrow>=12.0           # read parquet Google shards

# Optional — needed for DeepAR / DRP / MQF2 (CWPDDA paper baselines)
# If not installed, these fall back to LSTM/GRU/CNN-LSTM automatically
# pip install "gluonts[torch]"
# gluonts[torch]>=0.14
